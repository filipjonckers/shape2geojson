# shape2geojson
Convert shape files to geoJSON:
- Put complete shape files (prj, dbf and cpg) in the *shp* directory
- Run the python script
- GeoJSON exported files will be created in a *output* directory
